// short hand notation
// trailing comma

var width = 5;
var height = 10;

var features = {
    call: true,
    sms: true
};

// alt + shift + arrow up/down
var m1 = {
    width,
    height,
    features,
};

var arr = [1,2,3,4,5]

console.log(m1);